<h1>Listing banners</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Banner url</th>
      <th>Banner image url</th>
      <th>App</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @banners.each do |banner| %>
      <tr>
        <td><%= banner.title %></td>
        <td><%= banner.banner_url %></td>
        <td><%= banner.banner_image_url %></td>
        <td><%= banner.app %></td>
        <td><%= banner.user %></td>
        <td><%= link_to 'Show', banner %></td>
        <td><%= link_to 'Edit', edit_banner_path(banner) %></td>
        <td><%= link_to 'Destroy', banner, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Banner', new_banner_path %>
